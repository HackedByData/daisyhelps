{
  "name": "daisyhelps-desktop",
  "version": "0.1.3",
  "private": true,
  "description": "Daisy Helps desktop app (Electron)",
  "author": "Daisy Helps",
  "main": "dist/main.js",
  "scripts": {
    "build:main": "tsc -p tsconfig.json && copyfiles -u 1 \"build/tray-icon.png\" dist",
    "build:renderer": "tsc -p tsconfig.renderer.json && copyfiles -u 2 \"src/renderer/**/*.{html,css,jsx}\" dist/renderer && copyfiles -u 2 \"src/renderer/assets/**\" dist/renderer && copyfiles -u 1 \"src/screen-picker.html\" dist",
    "build": "npm run build:main && npm run build:renderer",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron . --enable-logging",
    "release": "npm run build && electron-builder --win --publish never",
    "release:publish": "npm run build && electron-builder --win --publish always",
    "test": "vitest run"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "copyfiles": "^2.4.1",
    "electron": "^32.1.2",
    "electron-builder": "^25.1.7",
    "typescript": "^5.6.2",
    "vitest": "^2.1.4"
  }
}
